name: logistic-system

services:

  # =========================================================================
  # INFRASTRUCTURE
  # =========================================================================

  redis:
    image: redis:6.2-alpine
    container_name: logistic-redis
    hostname: redis
    ports:
      - '6379:6379'
    environment:
      - ALLOW_EMPTY_PASSWORD=yes
      - REDIS_DISABLE_COMMANDS=FLUSHDB;FLUSHALL
    networks:
      - logistic-system-network

  postgres:
    image: postgres:15
    container_name: logistic-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      PGDATA: /data/postgres
    volumes:
      - postgres:/data/postgres
      - ./initdb.sql:/docker-entrypoint-initdb.d/init.sql
    ports:
      - '5432:5432'
    networks:
      - logistic-system-network

  mongo:
    image: mongo:6-jammy
    container_name: logistic-mongodb
    ports:
      - '27017:27017'
    volumes:
      - mongo_data:/data/db
    networks:
      - logistic-system-network

  zookeeper:
    image: confluentinc/cp-zookeeper:latest
    container_name: logistic-zookeeper
    environment:
      ZOOKEEPER_CLIENT_PORT: 2181
      ZOOKEEPER_TICK_TIME: 2000
    ports:
      - '22181:2181'
    networks:
      - logistic-system-network

  kafka:
    image: confluentinc/cp-kafka:latest
    container_name: logistic-kafka
    ports:
      - '29092:29092'
    environment:
      KAFKA_BROKER_ID: 1
      KAFKA_ZOOKEEPER_CONNECT: zookeeper:2181
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka:9092,PLAINTEXT_HOST://localhost:29092
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: PLAINTEXT:PLAINTEXT,PLAINTEXT_HOST:PLAINTEXT
      KAFKA_INTER_BROKER_LISTENER_NAME: PLAINTEXT
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
    depends_on:
      - zookeeper
    networks:
      - logistic-system-network

  vault:
    image: hashicorp/vault:1.13.3
    container_name: logistic-vault
    ports:
      - '8200:8200'
    environment:
      VAULT_ADDR: 'http://0.0.0.0:8200'
      VAULT_DEV_ROOT_TOKEN_ID: 'root'
      VAULT_DEV_LISTEN_ADDRESS: '0.0.0.0:8200'
    cap_add:
      - IPC_LOCK
    networks:
      - logistic-system-network

  keycloak:
    image: quay.io/keycloak/keycloak:22.0.1
    container_name: logistic-keycloak
    command: start-dev
    environment:
      KC_HOSTNAME: localhost
      KC_HOSTNAME_PORT: 8080
      KC_HOSTNAME_STRICT_HTTPS: "false"
      KC_HTTP_ENABLED: "true"
      KC_HOSTNAME_STRICT: "false"
      KEYCLOAK_ADMIN: admin
      KEYCLOAK_ADMIN_PASSWORD: admin
    ports:
      - "8080:8080"
    networks:
      - logistic-system-network

  # =========================================================================
  # MONITORING
  # =========================================================================

  prometheus:
    image: prom/prometheus
    container_name: logistic-prometheus
    restart: unless-stopped
    command:
      - --config.file=/etc/prometheus/prometheus.yml
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml
    ports:
      - '9090:9090'
    networks:
      - logistic-system-network

  grafana:
    image: grafana/grafana
    container_name: logistic-grafana
    restart: unless-stopped
    environment:
      - GF_SECURITY_ADMIN_USER=grafana
      - GF_SECURITY_ADMIN_PASSWORD=password
    ports:
      - '3000:3000'
    links:
      - prometheus
    volumes:
      - grafana:/var/lib/grafana
    networks:
      - logistic-system-network

  # =========================================================================
  # MICROSERVICES
  # =========================================================================

  authentication-service:
    container_name: logistic-authentication-service
    image: truongtv1111/logistic-authentication-service:latest
    build:
      context: ./authentication-service
      dockerfile: Dockerfile
    environment:
      POSTGRES_URL: jdbc:postgresql://postgres:5432/postgres
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      REDIS_HOST: redis
      REDIS_PORT: 6379
    ports:
      - '8081:8081'
    depends_on:
      - postgres
      - redis
    networks:
      - logistic-system-network

  api-gateway:
    deploy:
      replicas: 1
    image: truongtv1111/logistic-api-gateway:latest
    build:
      context: ./api-gateway
      dockerfile: Dockerfile
    environment:
      ALLOWED_ORIGIN: http://localhost
      REDIS_HOST: redis
      REDIS_PORT: 6379
      DEFAULT_URI: http://authentication-service:8081
      AUTH_SERVICE_URI: http://authentication-service:8081
      LOGISTICS_SERVICE_URI: http://logistics-service:8093
      SHIPMENT_SERVICE_URI: http://shipment-service:8094
      TRACKING_SERVICE_URI: http://tracking-service:8095
      DRIVER_SERVICE_URI: http://driver-service:8096
      VEHICLE_SERVICE_URI: http://vehicle-service:8097
      NOTIFICATION_SERVICE_URI: http://notification-service:8098
    ports:
      - '4953:4953'
    depends_on:
      - redis
    networks:
      - logistic-system-network

  logistics-service:
    container_name: logistic-logistics-service
    image: truongtv1111/logistic-logistics-service:latest
    build:
      context: ./logistics-service
      dockerfile: Dockerfile
    environment:
      POSTGRES_URL: jdbc:postgresql://postgres:5432/shipment_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      KAFKA_SERVER: kafka:29092
      API_GATEWAY: http://api-gateway:4953
      SHIPMENT_SERVICE_URL: http://shipment-service:8094
      DRIVER_SERVICE_URL: http://driver-service:8096
      VEHICLE_SERVICE_URL: http://vehicle-service:8097
    ports:
      - '8093:8093'
    depends_on:
      - postgres
      - kafka
    networks:
      - logistic-system-network

  shipment-service:
    container_name: logistic-shipment-service
    image: truongtv1111/logistic-shipment-service:latest
    build:
      context: ./shipment-service
      dockerfile: Dockerfile
    environment:
      POSTGRES_URL: jdbc:postgresql://postgres:5432/shipment_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      KAFKA_SERVER: kafka:29092
      API_GATEWAY: http://api-gateway:4953
    ports:
      - '8094:8094'
    depends_on:
      - postgres
      - kafka
    networks:
      - logistic-system-network

  tracking-service:
    container_name: logistic-tracking-service
    image: truongtv1111/logistic-tracking-service:latest
    build:
      context: ./tracking-service
      dockerfile: Dockerfile
    environment:
      MONGODB_HOST: mongo
      MONGODB_PORT: 27017
      KAFKA_SERVER: kafka:29092
      API_GATEWAY: http://api-gateway:4953
    ports:
      - '8095:8095'
    depends_on:
      - mongo
      - kafka
    networks:
      - logistic-system-network

  driver-service:
    container_name: logistic-driver-service
    image: truongtv1111/logistic-driver-service:latest
    build:
      context: ./driver-service
      dockerfile: Dockerfile
    environment:
      POSTGRES_URL: jdbc:postgresql://postgres:5432/driver_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      API_GATEWAY: http://api-gateway:4953
    ports:
      - '8096:8096'
    depends_on:
      - postgres
    networks:
      - logistic-system-network

  vehicle-service:
    container_name: logistic-vehicle-service
    image: truongtv1111/logistic-vehicle-service:latest
    build:
      context: ./vehicle-service
      dockerfile: Dockerfile
    environment:
      POSTGRES_URL: jdbc:postgresql://postgres:5432/vehicle_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      API_GATEWAY: http://api-gateway:4953
    ports:
      - '8097:8097'
    depends_on:
      - postgres
    networks:
      - logistic-system-network

  notification-service:
    container_name: logistic-notification-service
    image: truongtv1111/logistic-notification-service:latest
    build:
      context: ./notification-service
      dockerfile: Dockerfile
    environment:
      POSTGRES_URL: jdbc:postgresql://postgres:5432/notification_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      KAFKA_SERVER: kafka:29092
      MAIL_HOST: smtp.gmail.com
      MAIL_PORT: 587
      MAIL_USERNAME: ""
      MAIL_PASSWORD: ""
      API_GATEWAY: http://api-gateway:4953
    ports:
      - '8098:8098'
    depends_on:
      - postgres
      - kafka
    networks:
      - logistic-system-network

  frontend:
    container_name: logistic-frontend
    image: truongtv1111/logistic-frontend:latest
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - '80:80'
    networks:
      - logistic-system-network

networks:
  logistic-system-network:
    external: false
    name: logistic-system-network

volumes:
  postgres:
  mongo_data:
  grafana:
